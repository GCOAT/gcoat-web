# Kore Fix-Back Log

Items discovered in gcoat-web that should be synced back to the Kore template.

---

## GA4 Integration Pattern

- **File:** `docs/kore-standards-frontend.md` (new section needed)
- **Problem:** No documented pattern for adding Google Analytics 4 to a Kore project
- **Fix applied here:** Created `frontend/assets/js/gtag.js` (external file, avoids inline scripts), added async gtag loader + local init script to all public pages before `</head>`, updated CSP on all pages (`script-src` + `connect-src` + `img-src` for `googletagmanager.com`, `google-analytics.com`, `region1.google-analytics.com`), added `window.gtag?.()` conversion event calls in form success handlers
- **Affects tiers:** v0 / v1 / v2

## Google Search Console Verification Pattern

- **File:** `docs/kore-runbook-deploy.md` (new section needed)
- **Problem:** No documented checklist for Search Console setup (verification meta tag placement, sitemap submission)
- **Fix applied here:** Added `<meta name="google-site-verification">` to index.html `<head>`, sitemap already generated by `scripts/seo.sh`
- **Affects tiers:** v0 / v1 / v2
